template: testify
packages:
  github.com/ivan-prykhodko/pgx-outbox:
    config:
      structname: "mock{{.InterfaceName}}"
    interfaces:
      Acknowledger:
      Dispatcher:
      Processor:
      Publisher:
      Repository:
      Router:
      RowQuerier:
